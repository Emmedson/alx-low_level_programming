#include "main.h"
/**
 * _abs - function to print sign of a number
 *
 * @int: character to be checked
 *Return: zero or number value
 */

int _abs(int)
{

	if (n < 0)
	{
		n = n * (-1);
	}

	return (0);

}
